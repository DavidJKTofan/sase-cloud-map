{
  "name": "sase-cloud-map",
  "version": "1.0.0",
  "devDependencies": {
    "prettier": "2.8.3",
    "wrangler": "^2.6.0"
  },
  "private": true,
  "scripts": {
    "start": "wrangler dev",
    "deploy": "wrangler publish",
    "format": "prettier --write  '*.{json,js}' 'src/**/*.{js,ts,html}'"
  }
}
